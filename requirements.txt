# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0
pydantic-settings>=2.0

# Database
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.14.0

# Auth
python-jose[cryptography]>=3.3.0
cryptography>=43.0
bcrypt>=4.0

# HTTP client
httpx>=0.27.0

# Utilities
python-dotenv>=1.0
python-multipart>=0.0.9

# OpenAI (for AI agents)
openai>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0

# Redis (for multi-instance rate limiting and caching)
redis>=5.0

# Azure SDK — Storage, Identity, Key Vault, Service Bus, Search, Monitoring
azure-storage-blob>=12.0
azure-identity>=1.15
azure-keyvault-secrets>=4.8
azure-servicebus>=7.12
azure-search-documents>=11.4
azure-monitor-opentelemetry>=1.0

# Payments
stripe>=8.0
razorpay>=1.4

# GraphQL
strawberry-graphql[fastapi]>=0.230

# gRPC
grpcio>=1.60
grpcio-tools>=1.60

# ML
scikit-learn>=1.4
