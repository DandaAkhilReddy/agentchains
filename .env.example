# ────────────────────────────────────────────────────────────────────────────
# AgentChains v1.0 — Environment Configuration
# ────────────────────────────────────────────────────────────────────────────

# Environment: development | test | production
ENVIRONMENT=development

# Marketplace Server
MARKETPLACE_HOST=0.0.0.0
MARKETPLACE_PORT=8000

# Database (SQLite for dev, PostgreSQL for production)
DATABASE_URL=sqlite+aiosqlite:///./data/marketplace.db
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/db?ssl=require

# Content Storage (local HashFS)
CONTENT_STORE_PATH=./data/content_store

# Auth — IMPORTANT: generate a strong random secret for production
# python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=change-me-to-a-random-string
JWT_ALGORITHM=HS256

# Payments
PAYMENT_MODE=simulated
X402_FACILITATOR_URL=https://x402.org/facilitator
X402_NETWORK=base-sepolia

# OpenAI (for AI agents)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o

# Billing
PLATFORM_FEE_PCT=0.02
SIGNUP_BONUS_USD=0.10

# MCP Server
MCP_ENABLED=true
MCP_RATE_LIMIT_PER_MINUTE=60

# CDN
CDN_HOT_CACHE_MAX_BYTES=268435456
CDN_DECAY_INTERVAL_SECONDS=60

# OpenClaw Integration
OPENCLAW_WEBHOOK_MAX_RETRIES=3
OPENCLAW_WEBHOOK_TIMEOUT_SECONDS=10
OPENCLAW_WEBHOOK_MAX_FAILURES=5

# Redemption (USD minimums)
REDEMPTION_MIN_API_CREDITS_USD=0.10
REDEMPTION_MIN_GIFT_CARD_USD=1.00
REDEMPTION_MIN_BANK_USD=10.00
REDEMPTION_MIN_UPI_USD=5.00

# CORS — comma-separated origins; use specific origins in production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ────────────────────────────────────────────────────────────────────────────
# Azure Services (v1.0)
# ────────────────────────────────────────────────────────────────────────────

# Azure Key Vault
# AZURE_KEYVAULT_URL=https://agentchains-kv.vault.azure.net/

# Azure Service Bus
# AZURE_SERVICEBUS_CONNECTION=Endpoint=sb://agentchains.servicebus.windows.net/;SharedAccessKeyName=...

# Azure AI Search
# AZURE_SEARCH_ENDPOINT=https://agentchains-search.search.windows.net
# AZURE_SEARCH_KEY=your-search-admin-key
# AZURE_SEARCH_INDEX_PREFIX=agentchains

# Azure Blob Storage
# AZURE_BLOB_CONNECTION=DefaultEndpointsProtocol=https;AccountName=...
# AZURE_BLOB_CONTAINER=content-store

# Azure Application Insights
# AZURE_APPINSIGHTS_CONNECTION=InstrumentationKey=...;IngestionEndpoint=...

# Redis (local or Azure Cache for Redis)
# REDIS_URL=redis://localhost:6379/0
# For Azure: REDIS_URL=rediss://:password@host.redis.cache.windows.net:6380/0

# ────────────────────────────────────────────────────────────────────────────
# Payment Providers
# ────────────────────────────────────────────────────────────────────────────

# Stripe
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_CONNECT_ENABLED=false

# Razorpay
# RAZORPAY_KEY_ID=rzp_test_...
# RAZORPAY_KEY_SECRET=your-key-secret

# ────────────────────────────────────────────────────────────────────────────
# Protocols (v1.0)
# ────────────────────────────────────────────────────────────────────────────

# A2UI Protocol
A2UI_ENABLED=true
A2UI_MAX_SESSIONS_PER_AGENT=10
A2UI_RATE_LIMIT_PER_MINUTE=60

# MCP Federation
MCP_FEDERATION_ENABLED=true
MCP_FEDERATION_HEALTH_INTERVAL_SECONDS=30

# Orchestration
ORCHESTRATION_ENABLED=true
ORCHESTRATION_MAX_CONCURRENT_WORKFLOWS=50
ORCHESTRATION_DEFAULT_BUDGET_USD=10.0

# ────────────────────────────────────────────────────────────────────────────
# OpenTelemetry
# ────────────────────────────────────────────────────────────────────────────

OTEL_ENABLED=false
# OTEL_EXPORTER_ENDPOINT=http://localhost:4317
# OTEL_SERVICE_NAME=agentchains-marketplace
